import { Box, Input } from '@chakra-ui/react';
import React from 'react';

const Navbar = () => {
    return (
        <Box as='header'>
            <Input></Input>
        </Box>
    );
};

export default Navbar;
